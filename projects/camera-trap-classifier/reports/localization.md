# Localization report

314 sample images had ground-truth bbox annotations to compare against (of 633 total processed).

**Overall recall (IoU >= 0.5): 92.5%** (331/358 ground-truth animals detected)

This is a sanity check on a small sample, not the full detector evaluation from design spec section 5 (mAP, missed-detection analysis by condition) -- that's deferred to the evaluation stage once training/validation splits exist.